services:
  wedding-address-form:
    image: nginx:alpine
    container_name: wedding-address-form
    restart: unless-stopped
#    ports:
#      - "8090:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - proxy

networks:
  proxy:
    external: true
